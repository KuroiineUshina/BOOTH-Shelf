# 버전 관리 규칙

- 배포가 완료된 뒤 처음 시작하는 수정 작업에서만 패치 버전을 `0.0.1` 올립니다.
- 다음 배포가 완료되기 전까지는 수정 횟수와 관계없이 같은 버전을 유지합니다.
- `manifest.json`의 `version`·`version_name`, `package.json`의 `version`, 앱 화면의 표시 버전은 항상 동일하게 맞춥니다.

현재 개발 버전은 `1.0.5`입니다.
